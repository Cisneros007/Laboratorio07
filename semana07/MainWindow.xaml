<Window x:Class="semana07.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:semana07"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <DataGrid Name="datagrid" AutoGenerateColumns="False" Margin="142,27,142,126">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Invoice ID" Binding="{Binding invoiceID}" />
                <DataGridTextColumn Header="Customer ID" Binding="{Binding customerID}" />
                <DataGridTextColumn Header="Date" Binding="{Binding date}" />
                <DataGridTextColumn Header="Total" Binding="{Binding total}" />
                <DataGridTextColumn Header="Active" Binding="{Binding active}" />
                <DataGridTextColumn Header="Numero de factura" Binding="{Binding factura}" />
                
            </DataGrid.Columns>
        </DataGrid>

        <Button  Content="Button" Click="ButtonListarinvoice_Click"  HorizontalAlignment="Left" Margin="142,344,0,0" VerticalAlignment="Top" Height="40" Width="112"/>


    </Grid>
</Window>
